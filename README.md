# Personal Website

## Steps to Run Locally

1. Open up a Terminal and run the command `npm start` which will open the site in your default browser.
2. To convert photos from JPG to WebP format, open a terminal and run the command `npm run photos`
3. To add photos to the index for a given feature, in a terminal run the command `npm run index`
