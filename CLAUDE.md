# CLAUDE.md

## Git Commits

When creating commits, do not add a `Co-Authored-By` line. Commits should be attributed to Guerme only.

Only commit locally — do not push commits to the remote repository unless explicitly asked to.

Only commit locally if I have requested it in the prompt.
